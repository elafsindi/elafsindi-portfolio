"import { useLang } from '../context/LanguageContext';\nimport { useTheme } from '../context/ThemeContext';\nimport './Bento.css';\n\nexport default function BentoTheme() {\n  const { t, toggleLang } = useLang();\n  const { theme, toggleTheme } = useTheme();\n\n  return (\n    <div className=\"bento-wrapper\">\n      <div className=\"bento-container\">\n        {/* Header */}\n        <header className=\"bento-card bento-header\">\n          <div className=\"bento-logo\">&lt;ES/&gt;</div>\n          <div className=\"bento-actions\">\n            <button className=\"bento-btn\" onClick={toggleTheme}>\n              {theme === 'dark' ? '☀️ Light' : '🌙 Dark'}\n            </button>\n            <button className=\"bento-btn\" onClick={toggleLang}>\n              🌐 {t.nav.toggleLang}\n            </button>\n          </div>\n        </header>\n\n        {/* Hero Section */}\n        <section className=\"bento-card bento-hero\">\n          <h2>{t.hero.greeting}</h2>\n          <h1>{t.hero.name}</h1>\n          <p>{t.hero.description}</p>\n        </section>\n\n        {/* Stats */}\n        <section className=\"bento-card bento-stats\">\n          {t.about.stats.map(stat => (\n            <div key={stat.label} className=\"bento-stat\">\n              <h3>{stat.value}</h3>\n              <p>{stat.label}</p>\n            </div>\n          ))}\n        </section>\n\n        {/* Skills */}\n        <section className=\"bento-card bento-skills\">\n          <h3 className=\"bento-title\">{t.skills.tag}</h3>\n          <div className=\"bento-skills-list\">\n            {t.skills.categories[0].skills.map(skill => (\n              <span key={skill.name} className=\"bento-tag\">{skill.name}</span>\n            ))}\n            {t.skills.categories[1].skills.map(skill => (\n              <span key={skill.name} className=\"bento-tag\">{skill.name}</span>\n            ))}\n          </div>\n        </section>\n\n        {/* Projects */}\n        <section className=\"bento-card bento-projects\">\n          <h3 className=\
<truncated 1075 bytes>